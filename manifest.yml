declared-services:
  my-discovery:
    label: discovery
    plan: free
applications:
  - name: discovery-news-demo
    command: npm start
    path: .
    memory: 512M
    services:
      - my-discovery-service
